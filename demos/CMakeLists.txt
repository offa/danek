set(DEMO_COMMON_SRC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/common")

subdirs(encapsulate-lookup-api
        extended-schema-validator
        log-level
        recipes
        simple-encapsulation
        )

